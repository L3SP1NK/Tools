#!/bin/bash
ssh_pid=$(sudo pidof sshd)

if [ -n "${ssh_pid}" ]; then
	coproc termux-toast "Killing ${ssh_pid}"
	kill ${ssh_pid}
else
	coproc termux-toast "Starting SSH daemon..."
	sshd&&coproc termux-toast "OK"
fi
